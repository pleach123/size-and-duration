[package]
name = "size-and-duration"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pure functions for parsing and formatting byte sizes and durations"
readme = "README.md"

[dependencies]
